<div id="edition-sidebar">
  <h4 class="mb-0">Navigation</h4>
    <ul class="nav flex-column">

      <% if params[:action] == 'show' %>
        <li class="nav-item">
          <h4><%= link_to I18n.t('general'), guw.edit_guw_model_path(@guw_model, organization_id: @organization.id), class: "nav-link group" %></h4>
        </li>
      <% end %>

      <li class="nav-link group" >
        <h4 class="mb-0 mt-0" style="width: max-content;">Type d'Unité Oeuvre</h4>
          <ul class="nav flex-column">
            <% guw_model_guw_types = guw_model.guw_types %>
            <% guw_model_guw_types.each do |guw_type| %>
              <li class="nav-item">
                <a class="nav-link group" href="<%= guw_type_path(guw_type) %>">
                  <span>
                    <%= guw_type %>
                  </span>
                </a>
              </li>
            <% end %>
        </ul>
      </li>

    </ul>
  </div>