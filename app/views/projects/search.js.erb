$("#organization_projects").html("");
$("#organization_projects").html("<%= j(render :partial => 'organizations/organization_projects', object: [@organization, @projects]) %>");

//Mettre à jour l'url sans recharger la page
if (history.pushState) {
    var newurl = window.location.protocol + "//" + window.location.host + window.location.pathname + '?min=<%= @min %>&max=<%= @max %>&sort_column=<%= @sort_column %>&sort_order=<%= @sort_order %>&sort_action=<%= @sort_action %>&is_last_page=<%= @is_last_page %>&search_column=<%= @search_column %>&search_value=<%= @search_value %>';
    window.history.pushState({path:newurl},'',newurl);
}