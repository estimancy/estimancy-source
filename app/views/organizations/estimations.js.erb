//$("#search-estimation-list").html("<%#= j(render partial: "projects/estimation_overview") %>");

$("#search-estimation-list").html("");
$("#search-estimation-list").html("<%= j(render :partial => "projects/estimation_overview", object: [@organization, @projects], locals: { sort_column: @sort_column, sort_order: @sort_order, is_last_page: @is_last_page }) %>");

// Avec plusieurs search-column et search-value
if (history.pushState) {
//    var newurl = window.location.protocol + "//" + window.location.host + window.location.pathname + '?filter_version=<%#= @filter_version %>&min=<%#= @min %>&max=<%#= @max %>&sort_column=<%#= @sort_column %>&sort_order=<%= @sort_order %>&sort_action=<%#= @sort_action %>&is_last_page=<%#= @is_last_page %>&search_column=<%#= @search_column %>&search_value=<%#= @search_value %><%#= @search_string.html_safe %>';
    var newurl = window.location.protocol + "//" + window.location.host + window.location.pathname + '?min=<%= @min %>&max=<%= @max %>&sort_column=<%= @sort_column %>&sort_order=<%= @sort_order %>&sort_action=<%= @sort_action %>&is_last_page=<%= @is_last_page %><%= @search_string.to_s.html_safe %>';
    window.history.pushState({path:newurl},'',newurl);
}

$("#myModalLabel").html('<%= I18n.t(:label_add_status_change_comment) %>');

$(".load_modal[data-toggle=modal]").each(function() {
    $(this).attr('data-target', '#edit_component');
});

