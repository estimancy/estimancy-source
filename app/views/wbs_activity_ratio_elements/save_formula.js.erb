$("#notifications").html("<%= j(render "layouts/notifications") %>");
$("#formula_ratio_section").html("<%= j(render(:partial => 'wbs_activities/wbs_activity_ratio_elements_formula', :object => [@wbs_activity_ratio_elements, @selected_ratio])) %>");

//Redirect to the correct URL (Apply or SAVE)
//window.location.replace("<%#= @redirect_url_apply_or_save_path %>");
//window.location.href = "<%#= @redirect_url_apply_or_save_path %>";

$(location).attr('href','<%= @redirect_url_apply_or_save_path %>');
location.reload();