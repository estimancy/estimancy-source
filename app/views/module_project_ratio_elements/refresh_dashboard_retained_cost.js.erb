$('#<%= "#{@retained_effort_id}" %>').val(<%= @cost_value %>) ;

// ON DASHBOARD: UPdate the Effort-Breakdown Cost retained value when effort has changed
update_mp_ratio_element_retained_cost_value();