<% @element_parents_ids_cost_values.each do |mp_ratio_elt_id, values| %>
<% parent_retained_cost_id = "retained_cost_#{@level}_#{mp_ratio_elt_id}" %>
    $('#<%= "#{parent_retained_cost_id}" %>').val(<%= values[:cost] %>) ;
<% end %>

// ON DASHBOARD: UPdate the Effort-Breakdown Cost retained value when effort has changed
update_mp_ratio_element_retained_effort_and_cost_values();