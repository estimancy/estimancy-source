<div class="tabs">
  <ul>
    <li><a href="#tabs-1"><%= I18n.t(:languages) %></a></li>
  </ul>

  <div id="tabs-1">
    <div class="group_button">
      <%= link_to I18n.t('create_button'), new_language_path, :class => 'btn' %>
    </div>

    <table id="table_list_1" class="table tablesorterPager table-striped table-bordered table-condensed">
      <thead>
      <tr>
        <th><%= I18n.t(:name) %></th>
        <th><%= I18n.t(:local) %></th>
        <th class="action"><%= I18n.t(:actions) %></th>
      </tr>
      </thead>
      <tbody>
      <% @languages.each do |language| %>
          <tr>
            <td class="span1"><%= language.to_s %></td>
            <td class="span1"><%= language.locale %></td>
            <td class="center">
              <%= link_to '', edit_language_path(language), :class => 'icon-pencil', :title => I18n.t('edit') %>
              <%= link_to '', language, data: {confirm: I18n.t('are_you_sure')}, method: :delete, :class => 'icon-trash', :style => 'color:red', :title => I18n.t('delete') %>
            </td>
          </tr>
      <% end %>
      </tbody>

      <%= render :partial => 'layouts/footer_table' %>
    </table>

  </div>
</div>